# Task state

Last updated: 2026-09-01

## Groq node-routed tool correction

Work is active on the existing `feat/sahayi-deployment` branch from exact fetched live commit `1852883769dc97c213449e1836fd7a5a26f06a6e`. At preflight, local and remote `main`, `feat/sahayi-deployment`, and `test/sahayi-final` all matched that commit; deployment-branch divergence was `0/0`, no branch was created, and the eight pending files were reviewed as task-scoped earlier Groq work. No unrelated change, secret, or unexpected remote history was found or discarded.

The live HTTP 400 occurs before Groq returns a tool call because every request exposes all seven tools and their provider-facing schemas include unions, nullable values, a nested readiness-answer array, and `maxItems`. The correction preserves Groq Chat Completions local function calling and the bounded application-managed tool loop, but routes only the current application-selected phase: one catalogue tool for safety/intent; catalogue plus verified procedure for routing; readiness questions plus evaluation for interview; checklist plus synthetic preparation for automatic preparation; one simulated-status tool for explanation; and no tool for official handoff. Provider schemas are zero-argument closed objects with no `anyOf`, `oneOf`, nullable type, type array, nested answer array, or `maxItems`; no request exposes more than two tools. Validated locale, confirmed service, readiness answers, selected persona, and demo status are bound from server state into the unchanged strict canonical Pydantic executor, so Groq cannot generate citizen answers, persona/status values, facts, fields, or URLs.

The first request retains `tool_choice="auto"`, `parallel_tool_calls=False`, `temperature=0`, disabled streaming, and bounded completion output. After one validated node-allowlisted empty-argument call, Sahayi reconstructs the exact assistant tool-call message and matching named tool result, removes tools, and sends the final request with `tool_choice="none"`; strict `AgentModelOutput` validation and local fact/action/source/checklist/worksheet/status/handoff reconstruction remain authoritative. Greetings and obvious low-information input are answered locally in English, Hindi, and Malayalam. HTTP 400 logging adds only failed-generation presence, graph node, fixed exposed-tool names/count, round, tool-call count, safe status/classification, and HTTP status; failed-generation contents and all citizen/provider/tool/credential content remain excluded. No Procedure Pack, LangGraph deterministic journey, frontend behavior, dependency, environment variable, provider retry, alternate model, built-in/remote tool, persistence, or telemetry changed.

Local release gates pass: 247 backend tests; 86 frontend tests; focused provider, offline-evaluation, and orchestration suites; frontend lint, TypeScript, OCR integrity, and production build; intent-model integrity/evaluation; both active Procedure Packs and schema drift; `pip check`; zero-vulnerability `pip-audit` and `npm audit`; Render/workflow YAML parsing; `git diff --check`; and redacted secret/privacy/logging/persistence/telemetry/frontend-bundle scans. No-cache image `61d7e13acb7f9deebe5dc0af3bd50719f36f939655cb05a48df1e0742b268cbc` is 248,003,617 bytes, runs as UID/GID 10001, and passed root, health, safe AI-disabled public config, catalogue, same-origin, and no-store checks. The established exact-container browser suite passed 23 temporary screenshots across English/Hindi/Malayalam and 360/390/768/1280 widths with visible focus, no overflow, local intent without network requests, explicit confirmation, one-question readiness, automatic checklist/preparation, local-only values/edits, the exact `DEMO — NOT FOR SUBMISSION` watermark, pack-owned official handoff, one structured composer, same-origin OCR, Groq disclosure, demo boundaries, and session clearing. Visual review found no release-blocking issue; container logs contained no citizen value, OCR text, tool payload, provider output, document content, or credential. Commit/push, exactly one normal Render deployment, one consented hosted Groq journey, safe hosted log review, and conditional fast-forward promotion remain pending.

## Groq Chat Completions transport correction

Work is active on the existing `feat/sahayi-deployment` branch from exact clean fetched deployed commit `187b31aa2d268aecef32b4251a93924f1f1c5e06`. At preflight, local/remote `main`, `feat/sahayi-deployment`, and `test/sahayi-final` all matched that commit at divergence `0/0`; no branch was created and no unrelated change existed.

The production HTTP 400 occurs before any tool executes because the deployed adapter uses Groq's beta Responses interface with flattened provider-side strict tools and complex schemas. The correction replaces only that transport with `client.chat.completions.create`. Requests contain the fixed model, bounded validated system/history/current-user messages, exactly seven nested local function tools, automatic tool choice, disabled parallel calls and streaming, and `max_completion_tokens`. Responses-only request fields and function-output items are absent. The bounded loop requires exactly one choice, validates tool-call IDs/names/JSON arguments/budgets, executes the existing strict Pydantic-backed local tools, reconstructs only allowlisted assistant tool-call fields, sends documented named tool messages, and validates non-empty final JSON as `AgentModelOutput`. Provider failure remains no-retry and returns generic deterministic guidance with only allowlisted diagnostics.

Canonical application tool schemas remain strict and retain the local 30-answer `maxItems` limit. A separate provider copy omits top-level `strict` and all `maxItems`, nests `name`/`description`/`parameters` under `function`, converts readiness unions to `anyOf`, expresses nullable persona ID as string-enum-or-null `anyOf`, requires every object property, forbids additional properties, and is recursively validated before transport. No Procedure Pack, LangGraph node, form-preparation path, UI, dependency, privacy boundary, secret, or environment setting changed.

Local verification passes: 242 backend tests; 86 frontend tests; focused provider, offline evaluation, and LangGraph suites; frontend lint, TypeScript, OCR integrity, and production build; intent-model integrity; both active Procedure Packs and schema drift; `pip check`; zero-vulnerability `pip-audit` and `npm audit`; workflow/Render YAML parsing; `git diff --check`; and redacted secret/privacy/logging/persistence/telemetry/frontend-bundle scans. No-cache image `f79b54f5f79ab48013b2d7a43b5a312f87492d212915fb000464e59b77adf9c1` runs as UID/GID 10001 and passed root/hashed asset, health/public-config/catalogue/detail/readiness/checklist/conversation, same-origin, and no-store checks. The established browser suite passed 23 temporary `/tmp` screenshots across English/Hindi/Malayalam and 360/390/768/1280 widths with visible focus, no overflow, local intent without network requests, explicit confirmation, automatic preparation, local-only values/edits, one structured composer, same-origin OCR, Groq disclosure, demo boundaries, and session clearing. Direct contact-sheet review found no release-blocking visual issue. The container log scan found no credential, citizen, tool-payload, OCR, filename, or document content. Commit/push, test-branch fast-forward, Render deployment, the single hosted Groq tool-backed call, and conditional main fast-forward remain pending.

## Conversation-driven preparation implementation

Work continues on `feat/sahayi-deployment` from clean fetched remote commit `389aec3aabf54fdf1872412c1f36b33c9f365187`; `origin/feat/sahayi-deployment` and `origin/test/sahayi-final` matched at divergence `0/0`, while `origin/main` had no unique commits and was four commits behind. The earlier authorized Groq adapter/test work was preserved exactly in that commit. No unrelated worktree change, branch creation, merge, rebase, stash, reset, cleanup, or historical-branch restoration occurred.

The audit confirmed that readiness answers previously populated only readiness state. The `preparation` node always returned a disconnected bundled synthetic-persona worksheet; personal fields had no question/input/validation/source mapping; OCR confirmation carried only a document category and populated no sheet value; and the primary conversation exposed only the synthetic worksheet title. Citizens therefore could not complete a populated application-preparation record without leaving the conversation.

Implementation now extends the existing Procedure Pack assistance model as strict `preparation_fields`. Active fields require stable field/question IDs, complete English/Hindi/Malayalam label/question/why text, input and local-validation types, required/sheet/confirmation/edit flags, allowed value sources, reviewed source IDs, and optional readiness-question/document mappings. The graph accepts and revalidates only completed field IDs, selects the next unresolved required local field, fans out checklist plus a value-free sheet definition after each interview turn, and withholds official handoff until readiness and required structural field progress are complete. Aadhaar asks for the new address locally after its verified readiness sequence; Kerala asks local applicant name, gender category, and contact address after its six verified readiness categories. Identifier, bank, signature, and otherwise unsupported fields remain blank.

React now holds authoritative prepared-value records only in memory, including value, allowed source, confirmation, local validation, editability, and update revision. Confirmed readiness labels derive mapped fields; confirmed local OCR clue values populate only mapped fields; local edits update the preview; completed questions are skipped; the checklist and structural sheet refresh automatically; the free-text composer is absent during structured questions; and the populated printable sheet retains `DEMO — NOT FOR SUBMISSION`. Personal values, document suggestions, raw OCR, files, filenames, and canvases never enter the conversation API, LangGraph, Groq, logs, telemetry, or browser storage. Start Over, End Session, inactivity, language/procedure switching, unload, and unmount clear or discard memory-only values.

Complete local release gates pass: 233 backend tests, 86 frontend tests, focused preparation/OCR/LangGraph/provider coverage, 17 offline agent evaluations, frontend lint/TypeScript/production build, intent-model integrity/evaluation, both active Procedure Packs, schema drift, OCR asset integrity, workflow YAML parsing, `pip check`, zero-vulnerability `pip-audit`/`npm audit`, `git diff --check`, and redacted secret/PII/logging/persistence/telemetry/frontend-network/bundle scans. The reviewed Groq request still uses the sole existing adapter/client and current documented Responses/local-tool contract; every normal automated provider test remains mocked.

No-cache image `28de3f908e372c392a695633d93d910d25a747448f8bd36cf943c7ee7a882006` is 239,814,677 bytes, runs as UID/GID 10001, and passed clean startup, root/hashed frontend assets, all 22 same-origin OCR assets, safe AI-disabled public config, localized catalogue/detail and deterministic no-store conversation/API checks. The exact-container browser suite passed 23 temporary `/tmp` screenshots across English/Hindi/Malayalam and 360/390/768/1280 widths with visible focus/no overflow, zero local-intent requests, explicit confirmation, automatic preparation/checklist/handoff, personal values absent from request bodies, local editing, one structured composer, same-origin local OCR with no pre-confirmation API request, voice text fallback, demo disclosure, and session clearing. Logs contained only startup and path/status access records, no 5xx response or citizen/provider/document content; visual review found no release-blocking issue. Commit/push, test-branch fast-forward, one Render deployment, hosted verification, and main fast-forward remain pending.

The exact implementation commit `94bbc6ba1f76da46c08cc27593136a8b3118036f` was pushed normally to deployment/test branches and deployed once to the confirmed `Hackathons` workspace service. Render reported it live; hosted root, health, safe public config, catalogue, and conversation checks returned HTTPS 200 with `Cache-Control: no-store`, and the hashed bundle matched the container build. The first hosted browser pass exposed only a harness timing race: it waited for the immediately rendered catalogue heading but not the asynchronous service cards. After adding that specific readiness condition, the same single hosted deployment passed all 23 synthetic browser captures and reported optional AI available without making a live Groq call. The test-only wait correction must be published and replace the live exact commit before final main promotion; live Groq remains owner verification.

## Groq/LangGraph contract restoration

Release work continues on the existing `feat/sahayi-deployment` branch from exact fetched local/upstream commit `2fbc0d7339d60cbfd7c260609124b9639df93ad8` at divergence `0/0`, with `origin/main` unchanged at `256b8e4b5eb8663ed66ea7631f24a72885fa2c75`. The only initial pending files were the explicitly authorized `src/sahayi_api/agent.py` and `tests/test_agent.py`; no deleted historical branch was recreated and no unrelated work was discarded.

The Groq HTTP 400 contract failure was isolated to an array-valued JSON Schema `type` in the provider tool definition (the readiness answer value path), which Groq rejected with safe classification `invalid_request_error`, code `json_schema_invalid`, and the structural rejected-parameter path. Canonical schemas and Pydantic validation remain unchanged. The sole existing adapter now deep-copies each provider-bound tool schema and recursively converts valid `type` arrays to equivalent `anyOf` branches. Its request remains limited to the fixed `openai/gpt-oss-120b` model, instructions, application-owned input, the exact seven local tools, `tool_choice: auto`, `parallel_tool_calls: false`, `stream: false`, and bounded output. No retry, fallback model, built-in/remote tool, reasoning option, persistence, or second client was added.

Provider failures remain generic for citizens and deterministic guidance remains available. Server logs now distinguish bad request, authentication, permission, timeout, rate limit, server, and other API categories while admitting only allowlisted exception classes, HTTP status, narrowly allowlisted Groq error type/code/structural parameter, response status, graph round, and tool-call count. Citizen messages/history, prompts, provider messages/bodies/output, tool arguments/results, addresses, and credentials are never logged. Tests exercise successful and invalid execution for all seven registered tools: `list_supported_services`, `get_verified_procedure`, `get_readiness_questions`, `evaluate_readiness`, `build_personalized_checklist`, `prepare_synthetic_form_assistance`, and `explain_simulated_status`.

Local release gates pass: 230 backend tests, 86 frontend tests, focused provider/orchestration tests, 17 offline agent evaluations, frontend lint/TypeScript/build, intent-model integrity, both Procedure Packs, schema drift, OCR asset integrity, `pip check`, zero-vulnerability `pip-audit`/`npm audit`, YAML parsing, same-origin/no-store runtime checks, and redacted secret/PII/logging/persistence/telemetry/frontend-bundle scans. No-cache image `4c9c55d75b9485efc8615d8975d94ff401a90ab9ce8873858b4411c79bf4f614` runs as UID/GID 10001 and passed root, hashed assets, OCR, localized API, public-config, and deterministic AI-disabled checks. The established browser suite passed 23 temporary `/tmp` screenshots across English/Hindi/Malayalam and 360/390/768/1280 widths, with zero local-intent network requests, explicit confirmation, one-question readiness, automatic preparation/handoff, same-origin local OCR, voice text fallback, Groq disclosure, exactly-once UI coverage, and session clearing. Visual review found no release-blocking issue. Commit/push, test-branch fast-forward, one Render deployment, hosted API/live Groq verification, and main fast-forward remain pending.

## LangGraph and local document-assistance release

Phase 0 completed on `feat/sahayi-langgraph-orchestration`, created and pushed from the exact clean release baseline `256b8e4b5eb8663ed66ea7631f24a72885fa2c75`. After fetch, local/remote `main`, `feat/sahayi-deployment`, and the starting feature branch matched that commit at divergence `0/0`; `test/sahayi-final` was synchronized at ancestor `7f2c3ff6a64ac1e368aed1f10d4b4239bb02661d` with no unique work and remains safely fast-forwardable. No other worktree or uncommitted change was present.

The existing browser smoke passed before modification and captured 22 temporary `/tmp/sahayi-baseline` screenshots across English, Hindi, Malayalam and 360/390/768/1280 widths. Direct review confirmed the primary conversation still exposed the four-stage strip, repeated privacy/voice explanation, a persistent composer after completion, and a long checklist in the main flow. These images are untracked baseline evidence only and will not enter Git.

Implementation is complete locally. Start now opens a genuinely minimal conversation without the old service-card/module wall or progress strip. Raw intent stays local; known out-of-scope Aadhaar name changes fail closed. One typed 12-step LangGraph has safety/consent, intent clarification, verified routing, confirmed document evidence, deterministic interview/readiness, parallel checklist/preparation, explanation, and exact pack-owned handoff nodes. It has at most one optional Groq call and no checkpointer, store, database, server thread, LangSmith tracing, or telemetry. The strict no-store conversation-turn API revalidates all browser-carried IDs/answers and never accepts raw local text or OCR without the separate consented cloud event.

Optional browser-local document assistance uses pinned Tesseract.js 7.0.0, PDF.js 6.3.289, and English/Hindi/Malayalam data 1.0.0. Twenty-two checksum-verified same-origin runtime files total 52,493,148 bytes and load only after explicit action. JPEG/PNG/WebP/PDF MIME+signature validation, 10 MiB, 20-megapixel, three-page, one-job, 30-second page, and 75-second total limits fail closed. PDF pages use temporary canvases; identifier shapes are redacted; low confidence stays unknown. Raw bytes, filenames, OCR text, canvases, and workers are never uploaded/persisted/logged; only citizen-confirmed allowlisted evidence may reach the graph and remains unverified.

Complete local gates pass: 190 backend tests, 86 frontend tests, lint, TypeScript, production build, 17 offline-agent evaluations, intent-model integrity/evaluation, both pack validations, schema drift, workflow YAML parsing, `pip check`, zero-vulnerability `pip-audit`/`npm audit`, OCR checksums, `git diff --check`, and redacted secret/PII/logging/persistence/telemetry/frontend-network/bundle scans. The initial JS/CSS are 491,918/18,834 bytes (107,093/4,777 gzip); lazy OCR JS and PDF worker are 449,609 and 1,265,413 bytes, plus the 52,493,148-byte self-hosted runtime set. Final no-cache image `10a4a85db560c31dbe34bf6be025b82f69a7eb47cf85519a14db3e83264ecff2` is 239,765,523 bytes, runs as UID/GID 10001, and passed root/hashed assets, OCR assets, no-store health/public-config/catalogue/detail/readiness/checklist/preparation/conversation turns, exact handoff, and clean path-only logs. The exact-container browser suite captured 23 temporary screenshots and passed English/Hindi/Malayalam at 360/390/768/1280, visible focus/no overflow, zero local-intent requests, explicit confirmation, both services/ambiguity/unsupported behavior, voice text fallback, automatic preparation, real offline synthetic OCR with zero pre-confirmation API requests and bounded evidence only, Start Over/End Session/inactivity tests, and visual before/after review. The temporary container was removed. Commit/push, exact branch promotion, one Render deploy, hosted verification, main promotion, and safe feature cleanup remain.

## Conversation-first final release work

Active branch: `feat/sahayi-unified-conversation`, created from clean synchronized conversation-first baseline `7f2c3ff6a64ac1e368aed1f10d4b4239bb02661d`. At preflight, local/remote `main`, `feat/sahayi-deployment`, and `test/sahayi-final` all pointed to that baseline at divergence `0/0`.

Start now opens one primary conversation surface rather than a service or module screen. A browser-memory orchestrator combines the existing PII gate and local multilingual matcher, confirmation, verified procedure loading, one-question readiness evaluation, automatic checklist and synthetic worksheet preparation, and a source-owned official handoff. The composer, voice control, history, suggested answers, factual progress, Start Over, End Session, and secondary Browse Services remain on that surface. Detailed catalogue/procedure/demo views remain secondary. The UI no longer asks citizens to start readiness, build a checklist, or prepare a worksheet during the primary journey.

Cross-service routing is deterministic before any provider call in both boundaries. Inside a Kerala pension task, an unqualified address change offers only the loaded Aadhaar and pension catalogue entries; an explicit pension-record address request states that no separate verified procedure exists. Clear requests can switch only to a verified catalogue service. Unsupported requests do not invent guidance. The primary deterministic journey does not require Groq, so provider absence/failure cannot corrupt its task state.

Browser voice input/read-aloud is an explicit progressive enhancement using SpeechRecognition/webkitSpeechRecognition and speechSynthesis with `en-IN`, `hi-IN`, and `ml-IN`. It never auto-starts; text remains the complete fallback. Copy discloses possible browser/vendor processing. Transcripts are memory-only and pass through the existing PII gate before matching or assistant processing. Recognition and synthesis stop on navigation, language change, unmount, Start Over, End Session, and inactivity expiry. Pronunciation and complete accessibility coverage are not claimed.

`.github/workflows/procedure-monitor.yml` adds a bounded daily/manual read-only invocation of the existing allowlisted one-shot monitor with `contents: read`, no secrets, no repository mutation, immutable SHA-pinned official actions, bounded artifact retention, a concise summary, and visible failure for changed/error/missing-baseline states. All seven current baselines remain intentionally unset, so scheduled runs correctly require human review until authorized fingerprints are established. The workflow never changes facts, activates packs, builds, promotes, deploys, or contacts Render.

Complete local verification for the unified change passes 179 backend tests and 71 frontend tests, clean frontend lint/typecheck, production build, intent-model check/evaluation, pack validation/schema drift, `pip check`, zero-vulnerability `pip-audit` and `npm audit`, `git diff --check`, and redacted secret/persistence/logging scans. No-cache image `a4408e342c297b0ee0279db4bc431996c7ff0b69ff2f7798ed546cb86ca9c9ca` runs as UID/GID 10001 and passed root/asset, no-store health/public-config, catalogue, detail, readiness, checklist, worksheet, and verified-handoff checks. The browser suite produced 22 temporary screenshots and passed English/Hindi/Malayalam journeys at 360/390/768/1280 with visible focus, no horizontal overflow, voice text fallback, zero finder network requests, explicit confirmation, automatic preparation/handoff, synthetic demo/status, and clearing. Added tests cover full Aadhaar conversation, one-copy rendering, session clearing, microphone unsupported/denied states, and catalogue-only pension/address clarification. Commit/push, exact fast-forward promotion, Render deployment, and hosted verification remain.

## Production AI-guided turn correction

The clean worktree was inspected on `feat/sahayi-deployment` at `b58bf93`; the named local and remote Groq-agent/release branches currently point to the same commit. The reported production public config was read as agent-enabled with provider `groq` and model `openai/gpt-oss-120b`. A single consented reproduction of the exact non-sensitive turn later returned a successful generic clarification, so the original intermittent upstream/parsing reason cannot be reconstructed from historical logs: all provider, timeout, incomplete/malformed response, JSON/schema, tool, and budget failures previously collapsed to one unlogged fallback.

The correction adds safe server-only reason logging without citizen text, model output, tool arguments/results, client addresses, credentials, exception messages, or stack traces. Invalid provider/model configuration now disables availability rather than silently appearing valid, and surrounding whitespace is removed from the server-side key. A narrow pre-provider catalogue gate handles explicit cross-service matches and distinguishes the ambiguous address-change request inside Kerala pension between verified Aadhaar address update and pension-record-related help; it only offers loaded active service IDs and states that no separate verified pension-record address-change procedure exists. The frontend no longer renders the same latest assistant response from both transcript and structured-response state.

Regression coverage now includes a successful normal turn, provider failure and safe diagnostics, incomplete/malformed provider response, final JSON/schema rejection, cross-service intent, ambiguous unqualified address change, explicit pension-record address scope, configuration validation, and single fallback rendering. Verification passed with 179 backend tests and 61 frontend tests, frontend lint, TypeScript, production build, intent-model integrity, both active Procedure Pack validations, and generated-schema drift. No Procedure Pack fact, persistence boundary, dependency, deployment, merge, commit, push, or external configuration was changed.

## Release candidate

Active branch: `feat/sahayi-groq-agent`, created and initially pushed from exact clean synchronized release source `86ccd3a536ea7ef54194c26f814e5ecc90cd664c` after fetch and preflight. The verified ancestry remains linear through multilingual `25c22ca`, agentic assistance `49cade0`, procedure intelligence `89be9a83`, on-device intelligence `b22f86f3`, and release candidate `86ccd3a`. `main` and `feat/sahayi-deployment` remain unchanged at `05adbb60c54ff29f25c2657455bf502cdb274d0b`; `release/sahayi-submission-2026-08-29` and the public Render service at https://sahayi.onrender.com remain unchanged.

This feature selects GroqCloud behind a small provider adapter while retaining the pinned OpenAI Python package only as the documented compatible HTTP client. Provider, fixed `https://api.groq.com/openai/v1` base URL, and sole allowlisted `openai/gpt-oss-120b` model are application-controlled values. The `openai/` prefix is Groq's model namespace; `GROQ_API_KEY` remains the only provider credential. The request omits optional reasoning/provider-specific parameters and unsupported Groq Responses fields. The existing seven local tools, application-owned history, deterministic factual reconstruction, strict Pydantic validation, PII gates, consent, memory cleanup, no-retry/no-model-substitution policy, and bounded runtime controls remain intact. Provider failure still returns deterministic Sahayi guidance. No built-in browser search, code execution, MCP/remote tool, arbitrary function, Procedure Pack, canonical fact, translation corpus, intent dataset/artifact, government-monitoring behavior, simulated-service boundary, or UI behavior changed.

## Implemented boundaries

Sahayi supports Aadhaar address update and Kerala old-age-pension guidance in English, Hindi, and Malayalam. The browser-local service finder combines deterministic pack phrases with a bundled character 2–5-gram Multinomial Naive Bayes classifier and requires confirmation. FastAPI serves source-linked Procedure Packs, deterministic readiness/checklists, synthetic worksheets and demo status from one same-origin stateless container. The optional GroqCloud assistant is separately consent-gated and disabled unless the explicit flag and server-only Groq key are both present. Public configuration identifies only its fixed provider/model and effective availability. Source monitoring is offline, one-shot and human-reviewed. Citizen workflow state is memory-only and cleared by End Session/inactivity; no database, cookies, browser storage, analytics, telemetry, real form/submission/status, or government integration exists.

## Model and procedure integrity

The 81-row owned synthetic dataset remains split 45 train / 18 validation / 18 final test. Model SHA-256 is `fd8966853576dc1233a82908f93ce80a56d87537d0f0a82ef94d25a97adf54b4`; dataset SHA-256 is `14e56d1561584e6bfec420957d393740a9f3f6de53cbf6b965ad4a0084c58e62`. Canonical regeneration is byte-identical. The 18-example synthetic test accuracy remains 0.833333, macro F1 0.863248, abstention 0.111111, accepted-prediction accuracy 0.937500, and unsupported false-positive rate 0.166667. These are synthetic diagnostics, not verified real-world accuracy.

Active Procedure Pack digests remain UIDAI `595a62902a6145c82f02b9fbe361e7d8db5e34dea8d48e7e030aac7e43222bad` and Kerala `6eac396b47a82c2978b812cd4915dcf5dbc4ccac14ea8e4d93e2e57f28a53a76`. No pack fact changed. The UIDAI fee conflict remains unresolved and displayed; the Kerala amount remains omitted.

## Verification completed

- 172 backend tests and 60 frontend tests passed; focused provider/config/public-config/Render/documentation tests, frontend lint, TypeScript and production build passed.
- Offline agent evaluations (within the complete backend suite), model regeneration/drift/evaluation, pack validation and generated-schema drift passed.
- `pip check`, `pip-audit`, and `npm audit` passed; both audits found zero known vulnerabilities, with the local editable `sahayi-api` package appropriately skipped by `pip-audit`.
- Documentation checks found 15 valid local links, no broken local links, all documented command paths present, and `git diff --check` clean.
- Redacted current-tree and diff scans found no Groq/OpenAI key-shaped value, populated bearer header, private-key block, credential, or real PII introduced by the correction. Phone/email/identifier-shaped path hits remain synthetic gate fixtures, detector code, or numeric model/hash substrings. Persistence/telemetry additions were empty. The rebuilt frontend bundle contained no provider credential name, key prefix, placeholder, or fixed Groq endpoint; public config exposed the selected provider/model and no credential detail; `render.yaml` parsed successfully and contains only an unset `GROQ_API_KEY` prompt with `sync: false` and no value.
- No-cache container image `862f61427dc33b562820880bde8e6c62aae29cd23c644ba9a43947b16c27302b` built successfully and ran as UID/GID 10001. Health, public `groq`/`openai/gpt-oss-120b` metadata without secret details, disabled-agent deterministic fallback, and container cleanup passed without a live provider or government call.
- The established browser suite produced 20 temporary `/tmp` screenshots and passed English/Hindi/Malayalam checks at 360, 390, 768 and 1280 pixels, including the longer localized Groq disclosures, overflow, visible focus, local intent/no network/confirmation, deterministic flows, demo disclosure/status, trust text, End Session and agent-disabled state. Direct review of all three provider-disclosure screenshots found no release-blocking layout issue. Screenshots were not created in the repository.

## Manual/external work still required

Native-speaker/legal review of Hindi and Malayalam; representative privacy-reviewed real-world model evaluation, calibration, fairness/error and user accessibility review; and resolution/re-review of official-source limitations remain external. Groq currently lists `openai/gpt-oss-120b` on its free plan with indicative limits of 30 RPM, 1,000 RPD, 8,000 TPM, and 200,000 TPD; operators must check Groq Console for their exact organization limits. A Groq organization owner must separately review/enable Data Controls if Zero Data Retention is desired; Sahayi code cannot enable or guarantee it, and Groq documents usage-metadata collection. This task explicitly authorizes Git promotion after local gates; deployment, hosted verification, live government-source retrieval, any live/billable Groq call, and real submission/status remain unperformed.
